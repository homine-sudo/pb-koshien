# 俺らの甲子園 2026夏 ── 運用マニュアル

最終更新: 2026-07-31 ／ 対象: 第108回全国高校野球選手権（2026年夏・49校）

---

## 1. これは何か

仲間内でやっている「甲子園 順位予想ゲーム」の集計アプリ。
これまで `野球.xlsx` で手集計していたものを、単一HTMLのWebアプリに置き換えた。アプリ名は **「俺らの甲子園 2026夏」**（題字は筆文字）。

| 用途 | URL |
|---|---|
| **みんなに送る（閲覧）** | https://homine-sudo.github.io/pb-koshien/ |
| **集計担当だけ（管理）** | https://homine-sudo.github.io/pb-koshien/?admin=1 |
| デモ（動きを見せる用） | https://homine-sudo.github.io/pb-koshien/?demo=1 |

> **重要**：集計は必ず**公開URLの `?admin=1`** で行う。
> デスクトップにあるHTMLファイルと公開URLは、ブラウザ上では**別の保存場所**になる。
> 両方で入力するとデータが分裂するので、公開URL側に一本化する。
> 一度 `?admin=1` で開けば、以後その端末では「⚙️管理」タブが出続ける。

---

## 2. ゲームのルール

1. 参加者は**出場49校すべてに 1〜49 の点を重複なく振る**（合計は必ず **1225**）
2. **獲得ポイント = その校に振った点 × その校の勝利数**
3. 全試合が終わった時点で獲得ポイントの多い順に順位を決める
4. **飛び賞は 8位 と 10位**

### 点数配分のコツ（このゲームの構造）

- 勝ち上がる校に高い点を寄せるほど有利
- ただし**全員が同じ点を付けた校は、勝っても全員に同じだけ入るので順位が動かない**
- 差がつくのは「**人によって評価が割れている校**」が勝ったとき
- アプリの「予想」タブに **「差」列**（いちばん高い人と低い人の開き）と
  **🔥ここが勝つと順位が動く** を出しているので、そこが見どころ

---

## 3. 大会の構造と勝ち数

49校なので、こうなる。

```
1回戦 17試合（34校が登場／残り15校はシードで2回戦から）
2回戦 16試合  →  3回戦 8試合  →  準々決勝 4試合  →  準決勝 2試合  →  決勝 1試合
                                                              合計 48試合
```

1試合で1校が消えるので **試合数 = 49 − 1 = 48**。

### 優勝・準優勝の勝ち数（4通りある）

| | 1回戦から出た校 | シード校（15校） |
|---|---|---|
| **優勝** | **6勝**（6試合） | **5勝**（5試合） |
| **準優勝** | **5勝** | **4勝** |

つまり **(6,5) (6,4) (5,5) (5,4)** のどれもあり得る。
※春のセンバツ（32校）は5回勝てば優勝なので、**優勝5勝・準優勝4勝で固定**。夏とは違う。

---

## 4. 集計担当の作業（3ステップ）

### ステップ1：予想を集める（〜7/31 12:00）

1. LINEで届いた「`01. 白樺学園（北海道）：2`」形式のテキストをコピー
2. `?admin=1` → **「① 予想の取り込み」**
3. 参加者の名前を入れて、**テキストをそのまま貼り付け** → 「チェック」

> ⚠️ **名前は名簿と同じ表記にすること。** 本人の署名が違っても、名簿の表記で登録する。
> 例：名簿が `ケン＆でひ` なら、署名が「ケン＆でーひー」でも `ケン＆でひ` と入れる。
> 違う表記で登録すると、名簿側が永久に「まだの人」に残り続ける。

チェックが通ると：
```
✅ OK：49校すべて ・ 1〜49を重複なく使用 ・ 合計1225
   1位に置いた校：横浜（49点） ／ 最下位：横手（1点）
```

不備があれば行番号つきで止まる（登録されない）。検出できるもの：
- 点数の重複 → `点数 11 が 横手 と 遊学館 で重複`
- 範囲外 → `白樺学園 の点数「50」が1〜49の外です`
- 抜け → `点数が入っていない校が1校：東海大甲府` ＋ `使われていない点数：21`
- 番号と校名の食い違い → `6行目：番号(6)と校名(仙台育英)が食い違っています`

表記ゆれは自動で名寄せする：
`高岡商業→高岡商` `八幡商業→八幡商` `佐賀商業→佐賀商` `大分商業→大分商`
`鹿ヶ浦・霞ヶ浦→霞ケ浦` `関東一→関東第一` `智辯和歌山→智弁和歌山`

番号なし・半角コロンだけの形式でも通る。

### ステップ2：組み合わせを入れる（8/1 17:00の抽選後）

**「🗂対戦表」タブ** で、抽選結果を見ながら学校を入れる。

- 空欄の「＋ 学校を選ぶ」をタップ → 検索して選ぶ
- 1回戦17試合を入れる。2回戦以降は勝ち上がりが決まってから入れてもよい
- 3回戦までが抽選で決まる組み合わせ。**準々決勝以降は勝った学校が試合後にくじを引く**ので、その都度入れる

### ステップ3：大会中（8/5〜）

**基本、何もしなくていい。** 勝敗はスポーツナビから自動で取り込まれ、順位もみんなの画面も自動で更新される。

やることがあるとすれば：

- 校名が照合できずに自動取り込みが止まったとき（管理→③に校名が出る）→ 🗂対戦表で手入力
- 対戦表で直したいとき：**勝った学校をタップ**（もう一度タップで取り消し／反対を押せば付け替え／長押しで学校の入れ替え）

> 共有リンク（⑤）は予備の手段。ふだんは使わない。
> みんなに送るURLは `https://homine-sudo.github.io/pb-koshien/` の**1本だけで、貼り直し不要**。

---

## 5. 画面の見方

### 🏆 順位
獲得ポイントの多い順に並ぶ。

- 順位 / 名前 / 獲得ポイント。バーの長さで差が見える
- 順位が変わると行がスライドして入れ替わり、名前の横に **▲1 / ▼2** が出る
- ポイントはカラカラと数字が回って止まる
- 8位と10位に **「飛び賞」** バッジ
- 名前をタップすると、その人の「獲得できた校」と「これから伸びる可能性」が見られる

### 🗂 対戦表
ラウンド別のトーナメント表。勝った学校が緑＋✓、負けた学校が取り消し線。

### 🏫 出場校
49校の勝利数・敗退状況。校をタップすると**誰がその校を高く買っているか**が出る。

### 📋 予想
エクセルと同じ形式のマトリクス。

- **行＝49校／列＝参加者**（列は現在の順位順）
- 各参加者の下に **「予想」と「獲得」の2列**
- **配点は色のグラデーション**（低い＝寒色 → 高い＝暖色）。表の下に凡例バー
- **金枠**＝その校をいちばん高く買っている人
- **「差」列**（紫）＝いちばん高い人と低い人の開き。**差が小さい校は勝っても順位が動かない**
- 校名の列と見出しは固定なので、横スクロールしても軸が消えない

### ⚙️ 管理（集計担当だけ）
① 予想の取り込み ／ ② 提出状況 ／ ③ 試合結果の手入力 ／ **④ 集計の点検** ／ ⑤ みんなに共有 ／ ⑥ データの保存

---

## 6. 集計の点検（④）

**結果を入れる経路が2つある**（対戦表 と ③手入力）ため、両方使うとズレる。
それを機械が毎回チェックしている。

正常なとき：
```
終了した試合          33 / 48
敗退した校            33 / 48
勝ち残っている校      16 校
対戦表で勝敗が入った  33 試合
✓ 食い違いはありません
```

ズレたとき：
```
🚨 対戦表と勝ち数が食い違っています 1校：白樺学園（表0勝→集計1勝）
⚠️ 勝ち数の合計 34 と 敗退した校 33校 が合いません（1試合＝1勝＋1敗退）
   [ 対戦表に合わせて作り直す ]   ← 押せば対戦表の内容で作り直される
```

チェックしている項目：

| 項目 | 内容 |
|---|---|
| 合計超過 | 勝ち数の合計が48を超えていないか |
| 敗退超過 | 敗退が48校を超えていないか |
| 勝敗の対応 | 勝ち数の合計 = 敗退した校の数（1試合＝1勝＋1敗退） |
| 上限 | 6勝を超える校がないか |
| 対戦表との一致 | 全49校について、対戦表から数えた勝数・敗退と一致するか |
| 重複出場 | 同じ学校が同じラウンドに2回入っていないか |
| 不正な勝ち上がり | 負けた学校が次のラウンドに入っていないか |

**「優勝」の演出は、全48試合が終わるまで出ない**（途中で1校だけ残った状態でも出さない）。

---

## 7. 困ったとき

**Q. 貼り付けたのに弾かれる**
A. エラーに行番号が出る。よくあるのは点数の重複と抜け。合計が1225にならないときは必ずどこかが違う。

**Q. 間違った勝敗を入れてしまった**
A. 対戦表で同じ学校をもう一度タップすれば取り消せる。反対の学校を押せば付け替わる。

**Q. 数字が合わない気がする**
A. 「④ 集計の点検」を見る。食い違っていれば具体的に指摘され、ボタンで直せる。

**Q. データが消えないか心配**
A. 「⑥ データの保存」で JSONファイルに書き出せる。大会中は1日1回書き出しておくと安全。
別の端末に移すときも同じファイルを読み込めばよい。

**Q. スマホでも集計できる？**
A. できる。ただし**集計する端末は1台に固定する**こと（端末ごとに別データになるため）。

**Q. みんなの画面に反映されない**
A. まず ⚙️管理 → ⑦ で **「未配信の変更があります」** が出ていないか見る。出ていたら「今すぐ配信」を押す。
それでも古いままなら、相手側のブラウザが古い版を掴んでいる可能性がある。画面上部に
**「新しい版があります [更新]」** が出るので押してもらう（出ないときは一度閉じて開き直す）。

**Q. 名簿の人がずっと「まだの人」に残る**
A. **登録した名前と名簿の表記が違う**。管理→②の一覧で実際の登録名を確認し、名簿かどちらかに寄せる。

**Q. 登録したのに人数が0のまま／消えている**
A. **保存されない場所で入力した**可能性が高い。次の2つは入力しても保存されない。

| 開いていた画面 | 目印 |
|---|---|
| デモ（`?demo=1`） | 上に **🧪 デモを見ています** の帯が出る |
| 共有リンク（`#d=…`） | 上に **🔗 共有リンクを見ています** の帯が出る |

いまはこの状態で登録しようとすると**警告が出て止まる**ようにしてある。帯の「本番を開く」を押してからやり直す。
デスクトップのHTMLファイルと公開URLも**別の保存場所**なので、片方に入っていないか確認する。

---

## 8. 技術メモ

| 項目 | 内容 |
|---|---|
| 形式 | 単一HTML（題字の筆文字だけ Google Fonts を読む。他は外部依存なし） |
| データの置き場所 | 各端末のブラウザ内（localStorage）＋ リポジトリの `data/` |
| 予想データの配信 | 集計担当の操作 → GitHub API → `data/pool.json` → 全員が3分おきに取得 |
| 試合結果の取得 | スポーツナビ → GitHub Actions（30分おき）→ `data/games.json` → 全員が取得 |
| データの取得先 | `raw.githubusercontent.com`（即時反映）。落ちたら Pages 側で拾う |
| 共有リンク | 予備の手段。データをURLの末尾に載せる（`#d=…`）。ふだんは使わない |
| リポジトリ | `homine-sudo/pb-koshien`（Public・GitHub Pages は main / root） |
| 編集用ローカル | `C:\Users\homin\pb-koshien` |
| 手元の控え | `OneDrive\デスクトップ\PB\甲子園予想2026夏.html` |
| 検証テスト | `_test_tally.js`（集計36項目）／ `_test_autoimport.js`（取り込み45項目） |
| 提出の事前チェック | `node _check_submission.js ファイル 名前` |

> **なぜ2系統に分かれているか**：試合結果は「誰でも取れる客観的な事実」なので Actions が自動で取ってくる。予想データは「集計担当しか持っていないもの」なので、集計担当のブラウザが配信キーを使って上げる。

### 更新のしかた
```
C:\Users\homin\pb-koshien\index.html を編集
  → node _test_tally.js  （テストを通す）
  → git add -A && git commit -m "..." && git push
  → 1〜2分で公開URLに反映
```

### 設定を変えたいとき（コード内）
| 変えたいもの | 場所 |
|---|---|
| 飛び賞の順位 | `PRIZE_RANKS = [8,10]` |
| 出場校 | `TEAMS`（校名・都道府県の配列。次の大会はここを差し替える） |
| ラウンド構成 | `ROUNDS`（1回戦17 / 2回戦16 / 3回戦8 / 準々4 / 準決2 / 決勝1） |
| 配点の色 | `HEAT_STOPS` |

---

## 9. 検証の記録（2026-07-29）

集計は根幹なので、機械で検証した結果を残しておく。

**`_test_tally.js` ── 36項目すべて合格**

- トーナメントの形（49校・48試合・ラウンド構成・試合数＝校数−1）
- 48試合をフルに走らせたときの不変条件
  - 消化試合48／敗退48校／勝ち残り1校
  - 優勝校が勝ち残りの1校と一致
  - 優勝の勝数が5か6、準優勝が4か5、しかもシード可否と整合
  - 6勝を超える校がない
  - **全49校について「対戦表から数え直した勝数」＝「集計の勝数」**
- 得点計算（`予想×勝利数`の総和と一致・降順・同点は同順位）
- 手入力でズラしたときに検出でき、ボタンで復元できる
- 取り込みの検証（重複・範囲外・抜けを弾く／合計1225）
- 共有リンクの往復（予想・勝ち数・敗退・対戦表がすべて一致）

**ランダム40大会 ── 全て不合格0件**
優勝・準優勝の勝ち数の4通りがすべて出現した。

```
6勝-5勝 : 12回      5勝-4勝 : 11回
5勝-5勝 :  9回      6勝-4勝 :  8回
```

---

## 10. これからの予定

| 日付 | やること |
|---|---|
| 7/31 12:00 | 予想の提出期限 → 11人ぶんを取り込む → **「みんなに公開する」** |
| 8/1 17:00 | 組み合わせ抽選（オンライン）→ 🗂対戦表に1回戦17試合を入力 |
| 8/5 | 開幕。**以後は自動**（勝敗も順位もみんなの画面も） |
| 8/22 | 決勝。全48試合が終わると優勝演出（紙吹雪）＋飛び賞（8位・10位）が出る |

### 提出状況（2026-07-31 時点）

| | |
|---|---|
| 提出ずみ | N ／ ケン＆でひ |
| まだ | 大良・むら・ひかる・一＆おぎ・ひさし・ともや・ぺい・なーりー・洋平 |

---

## 11. 試合結果の自動取り込み

**大会中は、何もしなくても順位が更新されます。**

```
スポーツナビ（日程・結果ページ）
   ↓ 30分おきに GitHub Actions が取りに行く
data/games.json（このリポジトリ）
   ↓ アプリが10分おきに読む
対戦表・勝ち数・順位が自動更新（みんなの画面も同じ）
```

- 取り込むのは **組み合わせと勝敗**。1回戦17／2回戦16／3回戦8／準々4／準決2／決勝1 の計48試合
- 画面の上に `📡 12試合 決着 ／ 取得 8/8 15:30` と出る
- **校名が1つでも照合できないときは取り込みを止める**（誤った学校に勝ちを付けないため）。管理タブにその校名が出るので、そのときだけ手入力する
- 何度取り込んでも同じ結果になる（重複して勝ち数が増えることはない）
- 手で直したいときは 管理→③ の「自動取り込み **OFF**」

### 自動が止まったら
1. 管理→③ の「今すぐ取り込む」を押す
2. それでも駄目なら OFF にして、対戦表で手入力（従来どおり動く）
3. GitHubの Actions タブで実行結果とエラーを確認できる

### 仕組みのファイル
| ファイル | 役割 |
|---|---|
| `scripts/fetch_games.js` | スポーツナビから48試合を取ってJSONにする |
| `.github/workflows/fetch-games.yml` | 30分おきに上を実行してコミットする |
| `data/games.json` | 取得結果（アプリはこれを読む） |
| `_test_autoimport.js` | 取り込みロジックの検証（36項目） |

---

## 12. 予想の書き換え防止

- 登録した予想は **提出日時つきで確定**される
- 差し替えようとすると確認が出て、**「差替N回」として記録が残る**
- 「予想」タブの **「提出の記録（確認コード）」** に、全員の提出日時と4桁の確認コードが並ぶ
- **自分のコードを覚えておけば、後から書き換えられていないか誰でも確かめられる**（1点でも変われば別のコードになる）
- 管理画面は **パスコード**で保護（`?admin=1` を初めて開いたときに設定する）

> 補足：他人が自分の端末で `?admin=1` を開いても、変えられるのは**その人の手元のコピーだけ**です。みんなが見る順位は集計担当の端末のデータから作られます。パスコードは「うっかり管理画面を触られない」ための鍵で、アプリ自体は公開されているので、本気で解析する人を止めるものではありません。**実際の抑止力は「提出日時＋確認コードが全員に見えていること」**です。

---

---

## 13. 公開までは全員に見せない（公平性）

**先に出した人が不利にならないように、集計担当が「公開」を押すまで予想も順位も見えません。**

### 締切カウントダウン

受付中の画面のいちばん上に、**提出締切までの残り時間**が1秒ごとに動きます。

```
        7/31 12:00 締切まで あと
        18 時間 59 分 06 秒
```

- 残り6時間を切ると**赤くなって点滅**します
- 締切を過ぎると「締切を過ぎました」に変わります
- 締切日時は 管理→② の **「提出の締切」** で変更できます（既定は 2026-07-31 12:00）

### 受付中（公開前）に見えるもの

| | 見える？ |
|---|---|
| 🏆 順位 | ❌ 代わりに **受付状況の画面**（何人提出したか・誰がまだか） |
| 📋 予想 | ❌ 「🔒 公開までお待ちください」 |
| 🏫 出場校 → 誰が高く買っているか | ❌ 隠す |
| 🏫 出場校（勝敗）・🗂 対戦表 | ✅ 見える（予想と関係ないので） |

受付中のホーム画面はこうなります。

```
        予想を受付中
          3 / 11 人
       ███░░░░░░░░

  提出ずみ   [N] [洋平] [ぺい]
  まだの人   大良 むら ひかる 一＆おぎ …

  みんなの予想と順位は、全員の提出がそろって
  公開されるまで見られません。
  先に出した人が不利にならないようにするためです。
```

**共有リンクも非公開のまま渡ります**（リンクを先に配っても中身は見えません）。

### 公開の流れ

1. 管理→**② 提出状況と公開** の欄に、**参加する人の名前を1行ずつ**入れて「この名前で受付する」
   → 誰がまだ出していないかが全員の画面に出るようになります
2. 予想が届くたびに「① 予想の取り込み」で登録
3. 全員そろうと **「全員そろいました。公開できます。」** と出る
4. **「みんなに公開する」** を押す → その瞬間からみんなが予想と順位を見られます

> まだ全員そろっていなくても公開はできます（確認が出ます）。
> 間違えて公開したときは「公開を取り消す」で受付中に戻せますが、**すでに見た人には見えてしまっている**点に注意。

---

---

## 14. みんなへの配信（予想データ）

**集計担当が登録・公開すると、自動で全員の画面に反映されます。** リンクを貼り直す必要はありません。

```
集計担当が「登録」を押す
   ↓ ブラウザが GitHub に書き込む（配信キーを使用）
data/pool.json（予想・名簿・締切・公開状態）
   ↓ 全員のブラウザが3分おきに読む
みんなの画面が更新される
```

### 配信キー（GitHubトークン）

一度だけ設定します。**⚙️管理 → ⑦ みんなへの配信** に貼るだけです。

作り方（https://github.com/settings/personal-access-tokens/new）：

| 項目 | 設定 |
|---|---|
| Repository access | **Only select repositories** → `pb-koshien` |
| Permissions → Contents | **Read and write** |
| Metadata | Read-only（自動で付く） |

- キーは**その端末のブラウザにだけ**保存され、公開ページには入りません
- できるのは `pb-koshien` のファイル書き換えだけ。他のリポジトリやアカウントには触れません
- **キーをチャットやLINEに貼らないこと**（貼ってしまったら上のページで Delete して作り直す）
- 期限切れになったら、同じ手順で作り直して貼り替える

### 配信がうまくいかないとき

| 表示 | 対処 |
|---|---|
| 「まだ誰にも届いていません」 | 配信キーが未設定。⑦で設定する |
| 「未配信の変更があります」 | ⑦の **「今すぐ配信」** を押す |
| 「配信できませんでした：GitHub 401/403」 | キーの期限切れか権限不足。作り直す（Contents: Read and write） |

配信できているかは、この2つで確認できます。

- https://github.com/homine-sudo/pb-koshien/commits/main に `予想データ更新（N人・受付中）` が並ぶ
- https://raw.githubusercontent.com/homine-sudo/pb-koshien/main/data/pool.json が開ける

### 反映の速さ

- 配信は**押した直後**にリポジトリへ届く
- 各自の画面は**3分以内**（画面を開き直せばすぐ）
- ※ GitHub Pages 側は再ビルドに数分かかるため、アプリは raw から直接読んでいる

---
