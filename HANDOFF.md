# 引き継ぎ書 ── 俺らの甲子園 2026夏

作成 2026-07-31 ／ 更新 2026-07-31 17:00 ／ 次のセッションのClaudeへ
**運用手順そのものは `甲子園予想_運用マニュアル.md`（＝リポジトリの `MANUAL.md`）が正。この文書は開発の引き継ぎ。**

---

## 0. 最優先：いま止まっていること

### 提出はすべて完了・公開済み（2026-08-01 本番データで確認）

`data/pool.json`（本番・raw）を実確認した結果：

- **10人全員 提出ずみ／`published: true`（公開済み）**
- 名簿は **10人**（`ぺい` はいない・`洋平` ではなく **`陽平`**）
  N・大良・むら・ひかる・一＆おぎ・**ひさし**・ともや・なーりー・陽平・ケン＆でひ
- **大嶺さん＝`ひさし`**。`N` は別人（`N` を上書きしないこと）

> ⚠️ **ローカル作業ツリーの `data/pool.json` は古いことがある。**
> 提出状況を答える前に、必ず raw を取りに行くこと（`git pull` かfetch）。
> 2026-08-01 に、古い控えを見て「4人しか提出していない」と誤って報告した。

`OneDrive\デスクトップ\PB\提出_2026夏\` の A〜D は**すでに登録済みの分の控え**。
本番の登録データと picks 配列で突合し、**4件とも完全一致**を確認した（A=ひさし／B=ともや／C=むら／D=ひかる）。登録作業は残っていない。

### 【解決済 2026-08-06】スコアが1件も取れておらず、勝敗が永久に「未確定」だった

**症状**：8/5 に開幕し1回戦第1試合が終わったのに `games.json` は `decided: 0`。
`status` は「試合終了」なのに `homeScore/awayScore/winner` が全部 `null`。
中身が変わらないのでボットもコミットせず、**一見「順調に動いている」ように見えた**。

**真因**：スポーツナビの実HTMLは

```html
<span class="bb-score__score bb-score__score--left">3</span>
<span class="bb-score__score bb-score__score--center">-</span>
<span class="bb-score__score bb-score__score--right">1</span>
```

なのに `fetch_games.js` が `class="bb-score__score"` の**完全一致**で探していた。
1件もマッチせず `scores` が空 → `winner` が常に `null`。開幕まで誰も気づけなかった。

**対処**：`bb-score__score--(left|right)` で拾うよう修正。真ん中の `-` を混ぜないこと。
あわせて `counts.broken`（試合終了なのに勝敗が取れていない件数）を出すようにした。
**`broken` が 0 以外なら取得側が壊れている。** Actions のログにも警告が出る。

> 教訓：**「エラーが出ない＝動いている」ではない。** 数が増えるはずのものが増えていないかを見る。
> 8/6 時点の正常値は `paired 25 / decided 1 / broken 0`。

### 【解決済 2026-08-06 その2】試合が終わると、その試合がページから消えて結果が失われた

**症状**：スコアは取れるようになったのに、`games.json` の試合数が **48 → 47** に減っていた。
`1回戦` が 16 試合しかなく、**佐野日大（栃木）vs 聖隷クリストファー（静岡）が丸ごと消えていた**。
対戦表には「未定 vs 未定」の空行が1つでき、その試合の勝敗は永久に入らない。

**真因**：スポーツナビは**試合中・試合直後だけ**その試合の `<li>` に要素を足す。
`fetch_games.js` は `itRaw.split('</li>')[0]` で切っていたので、入れ子の `</li>` で
校名より前に切れてしまい `if(!home||!away) continue` で**黙って捨てていた**。
しかも毎回ゼロから書き直していたので、**一度読めた結果まで消えた**。

コミットを追うと交互に起きていた：

| 時刻(JST) | 8/6 第1試合 神村学園 | 8/6 第2試合 聖隷 |
|---|---|---|
| 13:30 | あり | あり |
| 17:32 | **消えた**（試合中） | あり |
| 20:21 | 戻った（5-1） | **消えた**（試合中） |

**対処**（3つとも入れた）
1. **前回ぶんと合体**するようにした。`id` をキーに、**空で上書きしない**。
   取れなかった試合は前回の内容がそのまま残る＝**結果は二度と消えない**。
2. 閉じタグで切るのをやめた。`chunks()` が「切っても中身が残るときだけ切る」。
3. `counts.expected`（48）と `ROUND_N` を持たせ、**足りなければログに警告**。
   アプリ上部のバーにも「48試合のうち N試合ぶんが取れていません」と出る。

消えていた 8/6 第2試合は `data/games.json` に戻した（勝敗は次のボットが入れる）。

**テスト**：`node _test_fetch.js`（偽のHTMLを fetch に返す。ネット不要）。
上の事故をそのまま再現しているので、直したつもりで壊したら落ちる。

> 教訓ふたたび：**減らないはずのものが減っていないか**を見る。件数は毎回 48。

### 【解決済 2026-08-06 その3】画面まわり2つ（大嶺さんからの指摘）

- **「横浜と神村学園の表示がうすい」**
  予想の表で `差 ≦ 6` の校を `.flat` として**校名ごと薄く**していた。
  ところが `.out`（敗退）も同じ薄さなので、**勝ち残っている校が敗退に見えていた**。
  横浜は差5、神村学園は差6。どちらも人気校で全員が高く買っている＝差が小さいだけ。
  → 校名は薄くせず、**「差」の欄だけ**薄くするようにした（`.pm tr.flat td.pm-g`）。
- **対戦表の並びが日付でばらばら**
  `buildFromGames` が試合番号だけで並べていたので
  8/5第1 → 8/6第1 → 8/7第1 …→ 8/7第2 … の順になっていた。
  → **日付 → その日の第N試合**で並べ、各行の左に **8/7 / 第3** と日付を出すようにした。

### 【追加 2026-08-06】トーナメント表を「枝分かれ図」にした

大嶺さんの指摘「トーナメント表これやで」（＝Yahooのあの図）に対応。

**データ**：`data/bracket.json`。**阪神甲子園球場の公式表**を書き写したもの。
<https://koshien.hanshin.co.jp/highschool/summer2026/tournament.html>

- `order` … 49校を**抽選どおりの並び**（上が1塁側）で上から
- `games` … 41試合（1回戦17／2回戦16／3回戦8）。各枠は
  `{"t":"高岡商"}`＝その校が直接入る（シード）／`{"w":"2026-08-05#1"}`＝その試合の勝者

> スポーツナビの `tournament/detail` は**中身が読めない**（JSで描いていて、
> HTMLを取ってもニュース欄しか出てこない）。日程ページだけだと
> 「勝ったあとどこへ行くか」が試合後にしか分からないので、公式表から起こした。
> 準々決勝から先は**勝った学校が試合後にくじを引く**ので、枝は引けない（図も3回戦まで）。

**準々決勝から先（2026-08-16 追加）**：`btLate()` が **2つ目の図**を作る。
準々決勝はくじ引きなので上の図とはつながらない（別枠にしてある）。
枝は固定せず、**決勝の顔ぶれ→準決勝→準々決勝の順にたどって毎回組み直す**ので、
くじがどう出ても正しくつながる。まだ抽選前のところは決まっている順に並ぶ。

**線の色（2026-08-13 追加）**：勝ち上がった経路だけを濃い緑（太め）にしている。
負けた側は元の灰色のままなので、**線をたどると「どこまで勝ち進んだか」がそのまま見える**。
縦線は真ん中で上下に分け、勝った側の半分だけ色を変えている（`.bt-v.won` など）。

**画面**：対戦表タブの上に「🗂 枝分かれ図 / ☰ 一覧」。既定は図。
左端に49校、右へ1回戦→2回戦→3回戦。勝った校は緑・負けた校は取り消し線、
枝の上に**勝者とスコア**が出る。自分の点も校名の横に出る。
**勝敗を手で直すときは「☰ 一覧」に切り替える**（編集は今までどおり一覧側）。

**テスト**：`node _test_bracket.js`。bracket.json と games.json を突き合わせ、
49校が1回ずつ・41試合が実在・校名一致・木をたどると order と同じ順、を確認する。
**書き写しミスは「勝ったのに次の枠に出てこない」形で出て気づきにくい**ので、必ず通すこと。

### 【2026-08-07】定期実行が数時間まるごと飛ぶ → cron の「分」をずらした

**症状**：スクリプトは正常なのに結果が入らない。8/7 の実際の稼働は
10:40 → **6時間なし** → （手動）→ 19:03 → **3時間なし** → （手動）。
第4試合が終わっても21:49時点で入っていなかった。

**原因**：間隔ではなく**混雑**。GitHubの定期実行は、みんなが指定する時刻ほど
遅れる・飛ぶ。`0,30` は**いちばん混む指定**だった。

**対処**：半端な分（`8,23,38,53`）に変え、あわせて15分おきにした。
日本時間 7:08〜23:53 を15分間隔でカバー（1日68回、間隔は常に15分）。
**`0` と `30` に戻さないこと。**

> それでも飛ぶことはある。遅れているかはアプリ上部のバーの
> 「取得 8/7 21:50」で分かる。急ぐときは
> Actions →「試合結果の取得」→ Run workflow で手動実行。

### 次にやること：日々の結果反映は自動（8/6〜）

修正後は30分おきのボットが勝敗まで取り込む。人の作業は不要。
ただし **1日1回は `counts.decided` が試合消化数と合っているか**、
**`counts.games` が 48 のままか**を見ること（アプリ上部のバーが黄色ければ何か足りない）。

### 【解決済 2026-08-13】雨で順延 → 対戦表が壊れた（大嶺さんの指摘「健大高崎のトーナメントがおかしくなってる」）

**何が起きたか**：8/12 第4試合 東海大甲府 vs 健大高崎 が雨で中止。
スポーツナビは中止の試合を残したまま、**別のIDで新しい試合を作る**。

| | id | 日付・試合 | 状態 |
|---|---|---|---|
| 元の試合 | 2021048506 | 8/12 第4試合 | 試合中止（勝敗なし） |
| 組み直し | **2021048619** | **8/13 第1試合** | 試合終了 1-0 |

これで3つ同時に壊れた：

1. **同じカードが2つ＝49試合**になり、2回戦が17試合に
2. `buildFromGames` は2回戦の枠を16個しか持たないので**17個目を捨てていた**。
   捨てられたのは **日南学園 vs 横浜（8/14）**。放っておけば翌日の結果が集計に入らなかった
3. 8/13 の試合番号が**全部1つずれた**（有明vs長崎日大 が 第1→第2 へ）。
   `bracket.json` は日付＋第N試合で試合を特定していたので、**枝が6か所迷子**になった

**対処**（日付・試合番号・試合IDのどれも当てにしない）

1. `fetch_games.js`：**中止のあと組み直された試合があれば、中止のほうを落とす**
   （同じ対戦カードで中止でないものが居るかで判定）。`counts.superseded` に件数を出す
2. `buildFromGames`：勝ち数・敗退を**枠ではなく取り込んだ試合そのもの**から数える。
   枠からあふれても勝敗は落とさない（同じカードは二重に数えない）
3. 枝分かれ図：`matchByDay(日付,番号)` をやめ、**対戦カード（2校の組）で実際の試合を引く**
   （`pairIndex()`）。日付・番号・IDが変わっても迷子にならない。表示する日付も実際のほうを使う
4. アプリ上部のバー：試合数が**多いとき**も警告するようにした（前は足りないときだけ）
5. `_test_bracket.js` も同じく対戦カード照合に書き換えた

> 教訓：**日付・試合番号・試合IDは変わる。変わらないのは「誰と誰が当たるか」だけ。**
> トーナメントは同じカードが二度と起きないので、対戦カードが最も安定した鍵になる。

### 【解決済 2026-08-13 その2】点数まわりを敵対的に検証 → 危険なボタンを1つ止めた

`_test_scoring.js` を追加（43項目）。**アプリの計算を信用せず、games.json から
独立に数え直して突き合わせる**。加えて嫌な入力を投げる：雨天中止→組み直し、
引き分け再試合、ホーム／アウェイ入れ替え、枠あふれ、勝者が出場校でない壊れたデータ、
同じカードの重複。得点そのものは**全員ぶん独立計算と一致**した。

**見つかった危険**：管理画面の整合性チェックに出る「**対戦表に合わせる**」ボタン。
順延で試合が増えて対戦表(48枠)に載りきらないとき、集計の勝ち数のほうが正しいのに、
このボタンは**表の側で上書きする＝勝ちが消えて全員の点が狂う**。

**対処**：`buildFromGames` が「枠に入りきらなかった決着ぶん」を `overflow` として返し、
取り込み時に `IMPORT_OVERFLOW` に記録する。**あふれているときは修正ボタンを出さず**、
「集計のほうが正しいので押さないでください」と注意だけ出す。
手入力でズラした場合（あふれていない）は今までどおり修正ボタンが出る。

> 得点は「配点 × 勝利数」の1行なので、**勝利数が1つずれると全員の点が静かにずれる**。
> 点を直接見張るより、勝利数の合計＝決着した試合数 を見張るほうが確実。

### 【追加 2026-08-11】優勝確率を全員に見せる

順位タブに「優勝確率」カードを追加した。**計算は各自のブラウザの中**で、
残り試合を5万回シミュレーションして「その人が1位で終わった割合」を出す。
サーバーは使わない（`runOdds()` / `renderOdds()`）。

- 1〜3回戦の枝は `data/bracket.json`、準々決勝から先はくじ引きなので毎回組み直す
- 抽選が済んだ回戦（`ST.matches` に両校が入っている）はその組み合わせを使う
- **強さ＝みんながその校につけた点の平均**。外部の戦力評価は持っていないので、
  手元にある唯一の手がかりを使っている。`ODDS_TEMP = 24`（平均配点の差20点で勝率7割）
- 速度：`Math.exp` を先に計算しておくことで **5万回が30〜80ms**。
  結果は `_oddsCache` に持ち、`oddsKey()`（消化試合数・名簿・勝ち数）が変わるまで再計算しない
- 公開前・抽選表なし・2人未満・全48試合終了、のときは出さない（デモでも出ない）

> ⚠️ **これは目安であって予想ではない。** 画面にもその旨を出している。
> 数字は試合ごとに大きく動く（8/8 ひさし 4.3% → 8/11 0.1%未満）。

### 【解決済 2026-08-11】飛び賞の順位

`PRIZE_RANKS = [8,10]` は11人前提で決めたもので、実際の参加者10人だと
**10位＝最下位**が飛び賞になっていた。大嶺さんの指示で **7位と9位** に変更した。

---

## 0-A. 貼り付け形式の罠（2026-07-31に実際に踏んだ）

**点数の前にコロン（`：`）が要る。** スペース区切りだと `parsePicks` の正規表現にマッチせず、
しかも `if(/[:：]/.test(z))` が false なのでエラーにもならず**黙って読み飛ばされる**。

実例：ある人の提出は全49行がスペース区切りで、コロンのある1行（聖隷）だけが読まれ
**「49校中1校・合計12」** になった。エラー0件と出るので気づきにくい。

```
✕ 01. 白樺学園（北海道）　　　19      ← 読まれない（エラーも出ない）
◯ 01. 白樺学園（北北海道）：19       ← これ
```

**対策**：みんなに配る記入テンプレートを「`01. 白樺学園（北北海道）：`」まで入った形にして、
数字だけ足してもらう運用にする（未実施・大嶺さんに提案済み）。
届いたテキストは必ず `node _check_submission.js ファイル 名前` に通してから登録する。

なお `鹿ヶ浦`（霞ケ浦の誤記）と `高岡商業/八幡商業/佐賀商業/大分商業` は `ALIAS` が吸収する。
県名の誤り（33.立正大淞南＝島根、34.鳥取城北＝鳥取 が入れ替わっている人がいる）は
判定に使われないので無害。

---

## 1. これは何か

仲間内11人の「甲子園 順位予想ゲーム」の集計アプリ。元は `野球.xlsx` の手集計。
**大嶺さんが集計担当。**

| | |
|---|---|
| 本番URL | https://homine-sudo.github.io/pb-koshien/ |
| 管理 | 同 `?admin=1`（パスコードあり・大嶺さんが設定済み） |
| デモ | 同 `?demo=1` |
| リポジトリ | `homine-sudo/pb-koshien`（Public・Pages は main/root） |
| 作業ツリー | `C:\Users\homin\pb-koshien` |
| 手元の控え | `OneDrive\デスクトップ\PB\甲子園予想2026夏.html` |
| 版 | `APP_VERSION = '2026-08-01'` |
| 大嶺さんの名前 | **ひさし**（アプリの「あなたは誰ですか？」でこれを選ぶ） |

**ルール**：49校に1〜49を重複なく配点（合計1225）／獲得P = 配点 × 勝利数／飛び賞は7位と9位

---

## 2. データの流れ（2系統に分かれている）

```
【試合結果】客観的な事実 → 誰でも取れる
スポーツナビ ─30分─> GitHub Actions ─> data/games.json ─> 全員のブラウザ

【予想データ】集計担当しか持っていない → 担当が押し上げる
大嶺さんのブラウザ ─配信キー─> GitHub API ─> data/pool.json ─> 全員のブラウザ
```

- 取得は **`raw.githubusercontent.com`**（即時反映・CORS許可済み）。Pages は再ビルドに数分かかるため保険
- 各自のブラウザは **3分おき**に両方を取りに行く
- 共有リンク（`#d=…`）は予備。ふだん使わない

### 配信キー
大嶺さんのブラウザ localStorage にのみ保存（`pb_koshien_gh`）。fine-grained PAT・`pb-koshien` のみ・Contents: Read and write。
**Claudeはトークンを見ない／扱わない。** 一度チャットに貼られたことがあり、その分は削除して作り直してもらった。

---

## 3. ファイル

| ファイル | 役割 |
|---|---|
| `index.html` | アプリ本体（単一HTML） |
| `MANUAL.md` | 運用マニュアル（大嶺さん向け・14章） |
| `scripts/fetch_games.js` | スポーツナビから48試合を取得 |
| `.github/workflows/fetch-games.yml` | 30分おきに上を実行してコミット |
| `data/games.json` | 試合結果（48試合・抽選前なので全部「未定」） |
| `data/pool.json` | 予想・名簿・締切・公開状態 |
| `_test_tally.js` | **集計の検証36項目** |
| `_test_autoimport.js` | 取り込み・公開ゲートの検証45項目 |
| `_test_scoring.js` | **点数の入り方の敵対的検証43項目**（独立計算と突合） |
| `_check_submission.js` | 届いた予想の事前チェック（`node _check_submission.js ファイル 名前`） |

### 触るときの手順（必ず守る）
```
index.html を編集
  → node _test_tally.js       （36項目）
  → node _test_autoimport.js  （45項目）
  → APP_VERSION を上げる
  → cp index.html "…/デスクトップ/PB/甲子園予想2026夏.html"
  → git add -A && git commit && git push
  → Pages反映を待って本番で実測（console 0 まで見る）
```

> ⚠️ `git push` の前に `git fetch` すること。**ボットや配信が勝手にコミットを積む**ので non-fast-forward で弾かれる。
> ⚠️ `.github/workflows/` は保存済みトークンに `workflow` 権限が無く push できない。変更時はGitHubのWeb UIから編集する。

---

## 4. 大嶺さんが決めたこと（蒸し返さない）

| 決定 | 補足 |
|---|---|
| **順位はシンプルな表** | かけっこ→競馬（楕円→直線）と作り込んだが「馬なしでシンプルに」で全撤去。**凝った演出より素直な表を選ぶ人** |
| 題字は筆文字で勢いよく | 「俺らの甲子園」／Yuji Boku＋一文字ずつ傾ける。Reggae One等は日本語グリフが出ず不採用 |
| 配点は連続グラデーション | 低=寒色→高=暖色。エクセルの4段階色分けが元ネタ |
| 公開ゲート | 全員そろって「公開」を押すまで予想も順位も見せない（公平性） |
| 完全自動配信 | 「共有リンクを貼り直す」方式は却下。設定15分をかけてでも自動を選んだ |
| 飛び賞は7位と9位 | `PRIZE_RANKS` |

**「集計は根幹だからちゃんとしてよ」** と明言されている。数字に関わる変更は必ずテストを通してから出すこと。推測でものを言わない。

---

## 5. 私（前セッション）がやらかしたこと

同じ轍を踏まないように残す。

1. **エクセルへの誤指摘** — 「2025夏シートは列が足りず集計が狂っていた可能性」と言ったが、実際は検討用コピーだった。**裏を取る前に言った**
2. **デモで入力が黙って消える** — `?demo=1` のリンクを何度も案内した結果、そこで登録され保存されなかった疑い。→ 警告して止めるように修正
3. **「このURLを貼れば全員見られます」が誤り** — 予想データは配信されていなかった。→ 自動配信を実装
4. **配信失敗が黙って落ちる** — `schedulePush` が silent で、失敗が管理タブの奥にしか出なかった。→ 全タブに赤帯＋再送ボタン（2026-07-31修正）
5. **実データが入る前に本番で動作テストした** — 紛らわしい状態を作った。**検証はデモ側で行う**
6. **（7/31）配点表を手で書いて点数を重複させた** — 大嶺さんに渡す49行を目で並べて出力し、46点を2校に振ってしまった。
   以後は**必ずスクリプトで生成して `_check_submission.js` に通してから貼る**。手で49行を書かない。
   点数を1校動かすときは「その校を抜いて挿し直し、間の校を1つずつ繰り上げ／繰り下げる」と順位を保ったまま1〜49を維持できる。

---

## 6. これからの予定

| 日付 | やること |
|---|---|
| ~~7/31 12:00~~ | 済：10人提出・公開 |
| ~~8/1 17:00~~ | 済：抽選反映。校名照合は `unmatched 0` を確認 |
| ~~8/5~~ | 済：開幕。**スコア取得の不具合を8/6に修正**（§0） |
| 8/6〜 | 自動。`counts.broken` が 0 のままかだけ見る |
| 8/22 | 決勝。全48試合終了で優勝演出＋飛び賞 |

### 8/1にやる検証（重要）
抽選後、スポーツナビに実際の校名が入る。`node scripts/fetch_games.js` を回して
`_test_autoimport.js` の `mapGames` で **照合できない校名が0件か**を確認する。
1件でもあると取り込みが止まる（fail-closed設計）。その場合は `ALIAS` に追加する。

---

## 7. 設定を変えたいとき（コード内）

| 変えたいもの | 場所 |
|---|---|
| 飛び賞の順位 | `PRIZE_RANKS = [7,9]` |
| 出場校 | `TEAMS`（次の大会はここを差し替え） |
| ラウンド構成 | `ROUNDS`（17/16/8/4/2/1） |
| 校名の表記ゆれ | `ALIAS` |
| 配点の色 | `HEAT_STOPS` |
| 締切 | アプリの 管理→② から変更可（`ST.deadline`） |

---

## 8. 集計の不変条件（壊さない）

- 勝ち数の合計 = 敗退した校の数（1試合＝1勝＋1敗退）
- 全48試合・49校・1校だけ勝ち残る
- 優勝は5勝か6勝、準優勝は4勝か5勝（シードかどうかで決まる。4通りすべてあり得る）
- 6勝を超える校は存在しない
- 全49校について「対戦表から数えた勝数」＝「集計の勝数」
- 校名が1つでも照合できなければ**取り込まない**（誤った学校に勝ちを付けない）
- 「優勝」演出は全48試合が終わるまで出さない

管理→④「集計の点検」が毎回これを検査している。ズレたら「対戦表に合わせて作り直す」で復元できる。
